# Matgate - full stack INCLUDING the browser farm ("via Chromium / Firefox VNC" websites).
#   docker compose -f docker-compose.browser.yml up -d
# Open http://localhost:8088 - the first start shows a setup wizard.
#
# Same as docker-compose.simple.yml, plus a browser-farm sidecar. Matgate still handles all its
# keys itself; the only farm setting is the one BrowserFarm__BaseUrl line that points at the farm.
name: matgate

services:
  edge:
    image: caddy:2
    depends_on: [matgate, guacamole]
    ports:
      - "8088:8088"
    entrypoint:
      - /bin/sh
      - -c
      - |
        printf "%s\n" \
          ":8088 {" \
          "  encode zstd gzip" \
          "  handle /guacamole* {" \
          "    forward_auth matgate:8080 {" \
          "      uri /internal/guac-authz" \
          "    }" \
          "    reverse_proxy guacamole:8080" \
          "  }" \
          "  handle {" \
          "    reverse_proxy matgate:8080" \
          "  }" \
          "}" > /tmp/Caddyfile && exec caddy run --config /tmp/Caddyfile --adapter caddyfile
    restart: unless-stopped

  matgate:
    image: ghcr.io/real-ttx/matgate:latest
    environment:
      # The only extra setting: turn the browser farm on by pointing Matgate at it.
      BrowserFarm__BaseUrl: http://browser-farm:8090
    volumes:
      - ./data:/data
      - matgate-secrets:/run/matgate-secrets
    extra_hosts:
      - "host.docker.internal:host-gateway"
    healthcheck:
      test: ["CMD-SHELL", "test -s /run/matgate-secrets/guac.key"]
      interval: 3s
      timeout: 3s
      retries: 20
    restart: unless-stopped

  guacd:
    image: guacamole/guacd:1.6.0
    restart: unless-stopped

  guacamole:
    image: guacamole/guacamole:1.6.0
    depends_on:
      guacd:
        condition: service_started
      matgate:
        condition: service_healthy
    environment:
      GUACD_HOSTNAME: guacd
      GUACD_PORT: "4822"
      JSON_ENABLED: "true"
    entrypoint:
      - /bin/sh
      - -c
      - export JSON_SECRET_KEY="$$(cat /run/matgate-secrets/guac.key)"; exec /opt/guacamole/bin/entrypoint.sh
    volumes:
      - ./data:/etc/guacamole
      - matgate-secrets:/run/matgate-secrets:ro
    restart: unless-stopped

  browser-farm:
    image: ghcr.io/real-ttx/matgate-browser-farm:latest
    init: true
    restart: unless-stopped

volumes:
  matgate-secrets:
